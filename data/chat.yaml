# ⚠️ 本文件已废弃 — 聊天配置已迁移到后端

# 原先 systemPrompt / panelTitle / models / suggestedPrompts 等配置都在这里，
# 由 Hugo 构建期渲染进 HTML（data-* 属性），导致 systemPrompt 明文暴露在前端源码。
#
# 2026-06-16 安全修复后：
#   - systemPrompt  → server/system-prompt.txt（仅后端内存持有，绝不下发前端）
#   - 展示类配置    → server/chat-meta.json（前端通过 /api/chat-config 异步拉取）
#
# 详情见 server/chat-api.mjs 的 readTextFile / readChatMeta / /api/chat-config 路由。
# 本文件保留仅为迁移说明，Hugo 不再读取 site.Data.chat。
